[report]
exclude_lines =
    # Don't complain about missing debug-only code
    def __repr__

    # Don't complain if tests don't hit defensive assertion code
    raise AssertionError
    raise NotImplementedError
    
    # Don't complain if test don't hit ImportError
    except ImportError:

    # Don't complain if non-runnable code isn't run
    if __name__ == .__main__.:

    # Don't complain debug only code
    if DEBUG:

omit =
    __init__.py
    src/kawaz/local_settings.sample.py
    src/kawaz/wsgi.py
    src/kawaz/core/management/*
